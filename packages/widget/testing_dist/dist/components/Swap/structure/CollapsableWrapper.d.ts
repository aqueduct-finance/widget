import { ReactNode } from "react";
interface CollapsableWrapperProps {
    defaultStyle?: string;
    children: ReactNode;
}
declare const CollapsableWrapper: ({ defaultStyle, children }: CollapsableWrapperProps) => import("react/jsx-runtime").JSX.Element;
export default CollapsableWrapper;
