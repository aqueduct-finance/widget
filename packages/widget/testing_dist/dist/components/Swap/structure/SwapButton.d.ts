import { Theme } from "../../../theme";
interface SwapButtonProps {
    swapTheme: Theme;
}
declare const SwapButton: ({ swapTheme }: SwapButtonProps) => import("react/jsx-runtime").JSX.Element;
export default SwapButton;
